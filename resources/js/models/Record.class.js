export default class REC {
    constructor(mat = "", qty = 0, single = 0, total = 0) {
        this.mat = mat;
        this.qty = qty;
        this.single = single;
        this.total = total;
    }
}